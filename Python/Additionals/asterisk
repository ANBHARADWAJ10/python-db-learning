the * means that any parameters after it must be specified using their name (as a “keyword argument”) when you call the function.
def run(playwright: Playwright, *, url: str) -> dict:
run(playwright, url='https://crawlee.dev')
